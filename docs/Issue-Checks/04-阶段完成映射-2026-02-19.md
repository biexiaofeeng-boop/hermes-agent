# Phase1 阶段完成映射（2026-02-19）

## 总览
- 已完成：T01 ~ T10（2026-02-22 全量收口）
- T04 / T05 / T06：已完成 prod 人工联调并回写证据
- T09 / T10：已合并并完成发布后联调

## 已完成项（代码已落地）
1. T01 部署生命周期
   - 证据：`deploy/chimera_core_deploy.sh`
   - 能力：`deploy/start/stop/restart/logs/health/status`
   - 补强：同项目 gateway 残留进程自动识别与清理（降低 Telegram polling 冲突）
2. T02 Toolchain Registry
   - 证据：`nanobot/chimera_bridge/toolchain.py`
   - 数据：`chimera-bridge/toolchain/registry.json`
3. T03 TaskOps + Board
   - 证据：`nanobot/taskops/hub.py`
   - 数据：`chimera-bridge/taskops/tasks.json`
   - 看板：`chimera-bridge/boards/YYYY-MM-DD.md`
4. T08 测试与回归
   - 证据：`tests/`
   - 一键命令：`deploy/chimera_core_test.sh`
   - 本地结果：26 tests passed

## 联调确认项（2026-02-22 已完成）
1. T04 Bot Dispatcher
   - 证据：`nanobot/taskops/services.py`
   - 验收结果：`closure-t04` 三条任务依赖链按序推进并全部 `done`。
2. T05 Human Notifier
   - 证据：`nanobot/taskops/services.py`
   - 验收结果：Telegram 通知触发成功，`lastNotifiedAt` 回写，冷却策略验证后恢复 4h。
3. T06 权限闸门
   - 证据：`nanobot/auth/gate.py`
   - 交互：`nanobot auth ...` + Telegram `/approve|/reject|/auth list`
   - 验收结果：strict 模式下完成默认拦截、once 单次放行、reject 回绝与审计链验证。

## 已完成的人工验收（2026-02-19 ~ 2026-02-22）
1. T01 部署生命周期（A）: PASS
2. T07 状态鉴权（B）: PASS（401/200）
3. T02 Toolchain Registry（C）: PASS
4. T03 Tasks + Board（D）: PASS
5. T08 回归测试（G）: PASS（14 tests）
6. T06 权限闸门：PASS（2026-02-22 strict 人工联调通过）
7. T04 Bot Dispatcher：PASS（2026-02-22，3 条 bot 任务 done）
8. T05 Human Notifier：PASS（2026-02-22，Telegram 通知 + 冷却验证通过）
9. T09（AuthGate v2）与 T10（TaskOps ControlPlane）：PASS（2026-02-21）

## 复验最小清单（可选）
1. `bash deploy/chimera_profile.sh restart`
2. `bash deploy/chimera_profile.sh health`
3. `bash deploy/chimera_auth_it.sh all`

## 下一个节点建议（Phase2）
1. 将 `taskops` 运行期配置逐步从 profile 全局项解耦到独立控制面配置。
2. 增加“任务描述/验收标准缺失”前置校验，减少 `blocked` 人工补录成本。
3. 扩展跨节点联调覆盖（网络稳定后补跑 `remote-acceptance`）。
4. 基于 run logs + audit logs 建立日/周自动健康报告。

## 2026-02-21 更新
1. T09（AuthGate v2）已完成验收：PASS（U09 deferred，不阻塞合并）。
2. 结论文档：`docs/Issue-Checks/2026-02/06-Checks-AuthGate-v2-2026-02-20.md`。
3. T10（TaskOps ControlPlane v1）已完成验收并合并：PASS（`master@7c03c60`）。
4. 发布后联调脚本已补齐：`deploy/chimera_auth_it.sh`，`main-smoke` 与 `child-check` 已人工 PASS。
5. 本地回归基线已提升：`deploy/chimera_core_test.sh` -> `54 tests ... OK (skipped=3)`。
