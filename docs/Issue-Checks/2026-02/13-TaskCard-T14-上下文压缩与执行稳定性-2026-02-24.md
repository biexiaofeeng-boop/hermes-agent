# 任务卡：T14 上下文压缩与执行稳定性

- 任务ID: T14
- 标题: Context Compression for Execution Stability
- 日期: 2026-02-24
- 负责人: chimera-core-codex
- 分支: `codex/t14-context-compression-exec-stability`
- 优先级: P1
- 状态: IN_PROGRESS（代码+单测完成，待实网长会话基线对比）

## 背景

当前会话历史直接注入最近 50 条，不做摘要，长上下文场景下执行稳定性下降。

## 目标

1. 将历史上下文改为“短窗 + 结构化摘要”。
2. 在不损失关键任务信息的前提下降低上下文噪声。
3. 为后续多代理调度预留标准化摘要接口。

## 范围

- In Scope:
  - 会话历史窗口下调（建议 16-20 条）。
  - 增加历史摘要（目标/完成/阻塞/下一步）。
  - 配置项可控（建议在 config schema 增加开关和窗口值）。
  - 单测覆盖长会话执行稳定性。
- Out of Scope:
  - 记忆系统（memory db）大改。
  - 外部向量数据库接入。

## 实施清单

- [x] S1: 在 `/Users/sourcefire/X-lab/chimera-core/nanobot/session/manager.py` 增加摘要生成接口（可先规则摘要，不引入额外 LLM 调用）。
- [x] S2: 在 `/Users/sourcefire/X-lab/chimera-core/nanobot/agent/loop.py` 使用新历史接口（短窗 + 摘要）。
- [x] S3: 在 `/Users/sourcefire/X-lab/chimera-core/nanobot/config/schema.py` 增加上下文窗口与压缩开关配置。
- [x] S4: 新增测试（建议 `tests/test_session_context_window.py`）。
- [ ] S5: 对比基线：同一批混合输入下，工具调用率与连续成功率提升。

## 验收标准

- [x] A1: 默认历史窗口 <= 20，且系统仍保留必要上下文连贯性。
- [ ] A2: 长会话（>=80 轮）下执行型输入工具调用率显著优于基线。
- [x] A3: 无新增明显幻觉/上下文错乱回归。
- [x] A4: 全量测试通过。

## 参考证据

- `/Users/sourcefire/X-lab/chimera-core/nanobot/session/manager.py:39`
- `/Users/sourcefire/X-lab/chimera-core/nanobot/agent/loop.py:225`
- `/Users/sourcefire/X-lab/chimera-core-prod/.runtime/profiles/prod/home/.nanobot/sessions/telegram_8464732775.jsonl`

## 进展记录

- 2026-02-25: 完成短窗 + 规则摘要实现，并接入 `AgentLoop` 历史构建路径。
- 2026-02-25: 新增配置项 `historyWindowMessages/contextCompressionEnabled/contextSummaryTriggerMessages`。
- 2026-02-25: 新增 `tests/test_session_context_window.py`；全量回归通过（`76 tests`）。
