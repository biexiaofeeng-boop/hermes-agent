# 实施顺序脚本清单：T17（v1）

- 目录: `/Users/sourcefire/X-lab/chimera-core`

## 0) 基线

```bash
cd /Users/sourcefire/X-lab/chimera-core
git status --short
bash deploy/chimera_core_test.sh
```

## 1) 分支准备

```bash
git checkout -b codex/t17-default-context-lobby-v1
```

## 2) M1：默认态 Lobby

```bash
# 关注点：移除 default_project 强回退 + loop 默认态透传
rg -n "default_project|_fallback|activeProject|should_update" nanobot/context/router.py nanobot/agent/loop.py
```

目标：
- 新会话默认 `activeProject=None`
- `/project switch` 与关键词路由不回归

## 3) M2：Lobby block + 灵魂锚

```bash
# 关注点：system prompt 组装层
rg -n "build_system_prompt|CORE_FILES|IDENTITY_CORE|Active Project|Memory" nanobot/agent/context.py

# 新增（建议）
# workspace/COMMAND_CENTER.md
```

目标：
- 无 activeProject 时注入 Command Center
- 灵魂锚点在默认态固定可见

## 4) M3：memory 轻载 + 配置

```bash
# 关注点：memory 注入路径与 schema
rg -n "get_memory_context|defaultMemoryMode|contextRouter|schema" nanobot/agent/memory.py nanobot/agent/context.py nanobot/config/schema.py
```

目标：
- 默认 `anchor_only`
- 可切换 `full`

## 5) M4：类型域优先路由

```bash
# 关注点：router 决策结构与关键词字典
rg -n "route\(|ProjectRouteDecision|keyword|alias|resolve_project" nanobot/context/router.py
```

目标：
- `domain-first` 判定落地
- 项目判定变二级

## 6) 测试与联调

```bash
# 定向
.venv/bin/python -m unittest tests.test_project_context_router tests.test_context_builder tests.test_project_context_assembler

# 兼容
.venv/bin/python -m unittest tests.test_auth_gate tests.test_taskops_controlplane tests.test_taskops_hub tests.test_taskops_services

# 手工链路
bash deploy/chimera_t15_m01_m04_it.sh

# 全量
bash deploy/chimera_core_test.sh
```

## 7) 文档回填

```bash
rg -n "T17|Lobby|domain|anchor" docs/Issue-Checks/2026-02/17-*
```

## 8) 收口门槛

- C01-C09 全部 PASS
- M01-M05 全部 PASS
- 无新增 P0/P1 回归

## 9) 协作提醒 Gate（新增）

- 需求补遗是否回写到 `17-问题描述-*` 与 `17-方案设计-*`
- 验收新增项是否回填到 `17-Checks-*`
- 合并前是否完成“升级路线复查”口头确认（你我互相提醒）
