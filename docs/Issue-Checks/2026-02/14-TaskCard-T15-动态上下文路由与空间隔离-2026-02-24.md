# 任务卡：T15 动态上下文路由与空间隔离

- 任务ID: T15
- 标题: Dynamic Context Loading/Unloading + Project Space Isolation
- 日期: 2026-02-24
- 分支: `codex/t15-project-context-router-v1`
- 优先级: P0
- 状态: DONE（T15-A~T15-E 通过，2026-02-25）

## 目标

1. 构建项目空间路由（active project）。
2. 上下文按项目动态加载，跨项目信息动态卸载。
3. 绑定 Auth mission workspace，实现执行隔离。
4. 让 TaskOps/Capabilities/Skills 跟随项目动态组合。

## 实施范围

- `/Users/sourcefire/X-lab/chimera-core/nanobot/agent/context.py`
- `/Users/sourcefire/X-lab/chimera-core/nanobot/agent/loop.py`
- `/Users/sourcefire/X-lab/chimera-core/nanobot/session/manager.py`
- `/Users/sourcefire/X-lab/chimera-core/nanobot/chimera_bridge/registry.py`
- `/Users/sourcefire/X-lab/chimera-core/nanobot/auth/gate.py`（联动字段）
- `/Users/sourcefire/X-lab/chimera-core/nanobot/taskops/*`（project 优先队列）
- `/Users/sourcefire/X-lab/chimera-core/chimera-bridge/context/packs/*`（新增）

## 实施清单

- [x] S1: 新增 `nanobot/context/router.py`（intent -> project）。（T15-A）
- [x] S2: 新增 `nanobot/context/assembler.py`（按项目 pack 组装）。（T15-B）
- [x] S3: `loop.py` 写入 session `activeProject` 并在 tool_context 透传项目上下文。（T15-A）
- [x] S4: 新增 context pack 目录与 `arb`/`wellness-tree` 两个 manifest。（T15-B）
- [x] S5: `taskops` claim 增加项目优先（当前会话 project 优先，fallback 全局）。（T15-B）
- [x] S6: 子代理 spawn 增加可选 project workspace（至少预留参数并打通到 ExecTool working_dir）。（T15-E）
- [x] S7: 增加 CLI/RPC：`context status`、`context switch <project>`、`context.projects`。（T15-D）
- [x] S8: 测试与文档回填。（T15-A：`tests/test_project_context_router.py` + `deploy/chimera_t15_m01_m04_it.sh`）

## 验收标准

- [x] A1: 输入“做 arb 套利回测”后 activeProject=arb。（2026-02-25）
- [x] A2: 输入“做儿童教育内容脚本”后 activeProject=wellness-tree。（2026-02-25）
- [x] A3: `arb` 空间下访问 `wellness-tree` workspace 路径被拦截（mission workspace 约束生效）。（2026-02-25）
- [x] A4: TaskOps 对当前 project 的 todo 任务优先 claim。（2026-02-25）
- [x] A5: 全量回归通过。（2026-02-25，`bash deploy/chimera_core_test.sh`）

## T15-A 收口记录（2026-02-25）

- 已实现：`/project switch`、session `activeProject` 持久化、关键词路由（arb/wellness-tree）、tool_context 透传 `activeProject`。
- 已验证：C01/C02/C04 通过；`deploy/chimera_t15_m01_m04_it.sh` 输出 `[FINAL] PASS`。
- 下一阶段（T15-B）建议：补 `context/assembler.py` + context packs + TaskOps project 优先 claim。

## T15-B 收口记录（2026-02-25）

- 已实现：`ProjectContextAssembler`、`chimera-bridge/context/packs/{arb,wellness-tree}/manifest.json`、TaskHub/ControlPlane/Dispatcher 的 `preferred_project` 优先 claim。
- 已验证：C03/C06 通过；相关单测：
  - `tests/test_project_context_assembler.py`
  - `tests/test_taskops_hub.py::test_claim_prefers_active_project_then_falls_back`
  - `tests/test_taskops_controlplane.py::test_claim_prefers_active_project`

## T15-C 收口记录（2026-02-25）

- 已实现：`loop.py` 在 tool_context 透传项目 `missionWorkspace/missionId/missionNode`（来源于 context pack `authPolicy`）。
- 已实现：`chimera-bridge/context/packs/*` 使用项目独立 workspace（`chimera-bridge/workspaces/{arb,wellness-tree}`）。
- 已验证：
  - `tests/test_auth_gate.py::test_guardrail_project_workspace_blocks_cross_project_path`
  - `tests/test_project_context_router.py::test_exec_tool_context_includes_project_workspace`
  - `bash deploy/chimera_core_test.sh`（90 tests 通过）。

## T15-D 收口记录（2026-02-25）

- 已实现：Context ControlPlane 与 Gateway RPC（`context.status` / `context.switch` / `context.projects`）。
- 已实现：CLI 命令 `nanobot context status|switch|projects`。
- 已验证：
  - `tests/test_context_controlplane.py`
  - `tests/test_cli_smoke.py::test_context_switch_and_status_smoke`
  - `bash deploy/chimera_core_test.sh`（94 tests 通过）。

## T15-E 收口记录（2026-02-25）

- 已实现：`loop.py` 将项目上下文传给 `SpawnTool`，并通过 `SubagentManager.spawn` 透传 `active_project/project_workspace/project_context`。
- 已实现：子代理 `ExecTool` 默认 `working_dir` 切换到项目 workspace（若存在），并在子代理工具上下文补齐 `missionWorkspace`。
- 已验证：
  - `tests/test_project_context_router.py::test_spawn_tool_passes_project_workspace_to_subagent`
  - `tests/test_subagent_project_workspace.py::test_run_subagent_uses_project_workspace_for_exec_tool`
  - `bash deploy/chimera_core_test.sh`（97 tests 通过）。

## 风险与回滚

- 风险1: 错路由导致上下文误装配。  
  - 缓解: 提供 `/project switch` 人工覆盖。
- 风险2: 隔离过严影响效率。  
  - 缓解: 保留 `mission` 可配 TTL 与临时提升。
- 回滚: 增加开关 `contextRouter.enabled=false` 回退到当前静态模式。
