# 需求说明：动态上下文加载与卸载（DCLU）v1

- 日期: 2026-02-24
- 面向对象: chimera-core-codex
- 优先级: P0（系统执行效率与可控性）

## 一、需求定义

你提出的判断完全正确：

> context 不是静态常驻，而应当围绕“当前项目空间”动态加载和卸载。

即：
- 进入 `arb` 空间时，加载套利目标/任务/能力/资源与风险约束；
- 进入 `wellness-tree`（教育 tree）空间时，加载教育对象/教学目标/叙事与内容策略；
- 其他项目上下文应被降权或卸载，避免互相污染。

## 二、现状（已具备与缺口）

### 2.1 已具备能力

1. **项目注册表** 已存在（可作为路由依据）：
   - `/Users/sourcefire/X-lab/chimera-core/chimera-bridge/registry/projects.json`
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/chimera_bridge/registry.py:67`
2. **TaskOps 有 project 字段**（可作为执行态绑定）：
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/taskops/hub.py:105`
   - `/Users/sourcefire/X-lab/chimera-core/chimera-bridge/taskops/tasks.schema.json`
3. **Capability / Executor 路由** 已有（可做项目化能力选择）：
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/taskops/feasibility.py:42`
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/taskops/router.py:39`
4. **Auth mission workspace 约束** 已存在（可做空间隔离底座）：
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/auth/gate.py:922`
   - `/Users/sourcefire/X-lab/chimera-core/nanobot/auth/gate.py:1782`

### 2.2 关键缺口

1. **缺少 Intent -> Project 路由器**（当前没有会话级 active project）。
2. **缺少 Context Pack 组装机制**（当前主要是静态核心文件 + 通用 bridge snapshot）。
3. **缺少 Context 卸载机制**（跨项目信息会持续滞留在会话历史）。
4. **缺少项目级工具/授权/任务联动编排**（context、auth、taskops、skills 尚未统一动态组合）。

## 三、目标能力（本轮定义）

1. 意图识别与空间路由（Intent Recognition & Routing）
2. 上下文组装器（Context Assembler）
3. 动态 Prompt 生成（Dynamic Prompting）
4. 项目空间隔离（Space Isolation）
5. 与 TaskOps/Auth/Skills 的动态编排（Policy Composition）

## 四、范围边界

- In Scope:
  - 项目空间路由、上下文动态加载与卸载。
  - 项目级任务/能力/授权联动规则。
  - 最小可用的空间隔离策略（mission workspace + working_dir 约束）。
- Out of Scope:
  - 向量数据库重平台化。
  - 全新 Web UI 管理台。
  - 多集群跨节点调度改造。
