# 并行执行排班表：S2-4 Lobby回锚与ActiveHours

- 任务ID: S2-4
- 日期: 2026-02-28
- 目标仓: /Users/sourcefire/X-lab/chimera-core
- 建议分支: codex/s2-4-lobby-anchor-active-hours

## 先回答你的问题（高优先级）

在 Codex 多 Thread 场景下：

1) 会话上下文（聊天记忆）默认是线程隔离，不自动共享。
2) 项目文件与 git 工作区是共享的（同一目录下所有线程看到同一份文件状态）。
3) 所以多线程协作要靠“文件事实源 + 明确文件归属 + 小步提交”来共享进展，而不是靠对话记忆。

## 线程拆分（建议 3 线程）

### Thread-A（会话与回锚主线）

- 负责人：A
- 目标：实现默认 lobby 与 idle 回锚
- 文件：
  - /Users/sourcefire/X-lab/chimera-core/nanobot/cli/commands.py
  - /Users/sourcefire/X-lab/chimera-core/nanobot/agent/loop.py
  - /Users/sourcefire/X-lab/chimera-core/nanobot/config/schema.py
- 交付：
  1. 默认 session 改为 cli:lobby
  2. 新增 /project clear 与 /lobby
  3. idleReturnToLobbyMinutes 配置与回锚逻辑

### Thread-B（Heartbeat activeHours）

- 负责人：B
- 目标：实现 quiet-hours 跳过
- 文件：
  - /Users/sourcefire/X-lab/chimera-core/nanobot/heartbeat/service.py
  - /Users/sourcefire/X-lab/chimera-core/nanobot/cli/commands.py
  - 测试文件（新增）
- 交付：
  1. activeHours(start/end/timezone) 判定
  2. 非窗口 heartbeat skip（仅日志，不触发 turn）
  3. 跨夜窗口测试

### Thread-C（文档与联调收口）

- 负责人：C
- 目标：回填文档、跑验收、形成发布建议
- 文件：
  - /Users/sourcefire/X-lab/chimera-core/docs/Issue-Checks/2026-02/21-Checks-S2-4-Lobby回锚与ActiveHours-v1-2026-02-28.md
  - /Users/sourcefire/X-lab/chimera-core/docs/Issue-Checks/2026-02/20-配置模板-S2-运营稳态-v1-2026-02-27.md
  - /Users/sourcefire/X-lab/chimera-core/docs/Issue-Checks/2026-02/00-INDEX-2026-02.md
- 交付：
  1. C01-C14 勾选与日志证据
  2. 配置模板更新（cli:lobby + activeHours + idle 回锚）
  3. 单一事实源索引更新

## 执行波次

- Wave-1（并行）：Thread-A + Thread-B
- Wave-2（收口）：Thread-C

说明：A/B 会同时修改 commands.py，需采用“先小改 + 快速 rebase + 再补测”的方式，避免冲突。

## 合并门禁

1. A/B 各自自测通过后再合并。
2. C 跑完整体验收后再打 DONE。
3. 24h 观察项（C14）未完成前，状态保持 CHECK。

## 冲突规避规则（必须）

1. 每线程只改自己责任文件，跨界前先在 Issue 文档登记。
2. commands.py 冲突按“功能块”合并，禁止一次性大面积重排。
3. 不做 stash、不切换到无关分支、不改其他线程正在改的文件。
4. 每次提交前先更新本线程文档中的“已完成/待完成”。
