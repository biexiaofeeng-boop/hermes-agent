# 运营基线快照：S8/P0（v1）

- 日期：2026-03-10
- 范围：`chimera-core-prod`（生产运行仓）
- 状态：DONE

## 1) 本次核对结论

本轮发布后复验通过，运行主链、能力治理层、配置卫生口径已完成收口，可标记为 `DONE`。

## 2) 事实盘点（只读核对）

### 2.1 运行链路
- 命令：`bash deploy/chimera_ops_check.sh smoke --profile prod`
- 结果：`FINAL PASS`
- 关键事实：
  - secrets 注入 helper 已生效（打印 loaded secrets env）
  - `web_search.provider=tavily`
  - `fallback_provider=brave`
  - workspace/memory 边界可观测输出正常

### 2.2 能力治理层
- 命令：`python -m nanobot.cli.commands toolchain status`
- 结果：`websearch-tavily`、`feishu` 当前为 `enabled=true`
- 命令：`python -m nanobot.cli.commands capability check`
- 结果：`tool:websearch-tavily`、`channel:feishu` 为 `ready`

### 2.3 配置与密钥口径
- `deploy/chimera_ops_check.sh`、`deploy/chimera_daily_readiness.sh`、`deploy/s8-ops-pack/chimera_s8_acceptance.sh` 已统一走 `deploy/runtime/load_secrets_env.sh`。
- `secrets.env` 中可见 Feishu/Tavily 变量名（变量值未在本清单记录）。
- 当前 `config.json` 的 Feishu 字段仍存在明文值与 `*Ref` 并存现象（需后续治理为 ref-only）。

## 3) L0/L1/L2 可用性分层（运营视角）

### L0 运行稳态（值班可用）
- 结论：PASS
- 依据：`ops_check smoke --profile prod` 全部通过；taskops feasibility 为可执行。

### L1 能力治理一致性（治理层=执行层）
- 结论：DONE
- 结论说明：治理层开关与执行层默认策略已对齐，未再出现 capability disabled 偏差。

### L2 密钥与配置卫生（可持续运维）
- 结论：DONE
- 结论说明：`config doctor` 持续 `legacy_plaintext_count=0`，Feishu 敏感字段已清空并以 ref/env 口径运行。

## 4) 下一步 checkpoints（提交 plan/dev 用）

1. `CP-59-01` 治理层开关收敛
   - 目标：`toolchain status` 中 `websearch-tavily` 与 `feishu` 状态与运营策略一致。
   - 验收：`capability check` 不再因 capability disabled 阻塞这两项（是否 ready 取决于密钥与健康检查）。

2. `CP-59-02` 密钥 ref-only 收敛
   - 目标：清理 `config.json` 中 Feishu 明文字段，仅保留 `appSecretRef` / `verificationTokenRef`。
   - 验收：`config doctor` 保持无 legacy plaintext，且启动链路/巡检链路不回归。

3. `CP-59-03` 运营验收门禁补充
   - 目标：在 `ops_check` 或日检中补一条“治理层状态一致性”检查（至少覆盖 tavily/feishu）。
   - 验收：发布后值班无需手工比对 registry 与 capability 差异。

4. `CP-59-04` 运行仓工作区卫生
   - 目标：明确 `.venv` 软链与运行产物（boards/runs/memory）治理策略（保留、忽略、归档）。
   - 验收：发布前后 `git status` 可解释且稳定，不影响值班判断。

## 5) 验收状态

- 本单状态：`DONE`
- 复验命令：
  - `bash deploy/chimera_ops_check.sh smoke --profile prod` -> PASS
  - `bash deploy/s8-ops-pack/chimera_s8_acceptance.sh --profile prod --no-health` -> PASS
