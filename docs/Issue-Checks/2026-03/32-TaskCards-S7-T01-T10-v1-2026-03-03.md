# 任务卡：S7（T01 ~ T10）

- 日期：2026-03-03
- 状态：READY

## S7-A Thinking 路由

### T01 配置模型扩展
- 文件：`nanobot/config/schema.py`
- 目标：新增 `agents.defaults.thinking_route` 配置结构。
- DoD：配置可加载，默认不改变现有行为。

### T02 AgentLoop 路由决策
- 文件：`nanobot/agent/loop.py`
- 目标：增加 `resolve_thinking_profile(...)`，输出 `fast/normal/deep` 与 reason。
- DoD：simple/complex 输入返回稳定 profile。

### T03 Provider 调用参数覆盖
- 文件：`nanobot/agent/loop.py`
- 目标：在 `provider.chat(...)` 调用注入按 profile 计算的 `max_tokens/temperature`。
- DoD：日志/metadata 可看到最终生效参数。

### T04 观测字段回填
- 文件：`nanobot/agent/loop.py`
- 目标：session metadata 记录 `lastThinkingProfile/Reason`。
- DoD：`/status` 或调试日志可见。

### T05 Thinking 路由测试
- 文件：`tests/*thinking*`（新增）
- 目标：覆盖 simple/complex/显式 difficulty 场景。
- DoD：测试通过，且 disabled 时行为与旧版一致。

## S7-B Feishu 工具化

### T06 Feishu 工具客户端抽象
- 文件：`nanobot/channels/feishu.py`（复用）或新建 `nanobot/channels/feishu_client.py`
- 目标：抽出可复用 token 与 API 请求层。
- DoD：channel 与 tools 共享同一认证逻辑。

### T07 新增 `feishu_chat` 工具
- 文件：`nanobot/agent/tools/feishu.py`（新增）
- 动作：`info` / `members`
- DoD：在工具调用链中可返回结构化结果。

### T08 新增 `feishu_doc` 工具
- 文件：`nanobot/agent/tools/feishu.py`（新增）
- 动作：`create_table_with_values` / `upload_file` / `upload_image`
- DoD：可完成最小文档自动化闭环。

### T09 工具注册与配置开关
- 文件：`nanobot/agent/loop.py`、`nanobot/config/schema.py`
- 目标：`tools.feishu.*` 开关控制注册。
- DoD：关闭时不暴露工具，开启时工具可用。

### T10 回归与文档回填
- 文件：`docs/Issue-Checks/2026-03/*`
- 目标：补充 checks、index、发布说明。
- DoD：索引与验收结论一致。

