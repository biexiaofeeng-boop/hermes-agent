# 任务卡：P0 能力与资源治理（T01 ~ T14）

- 日期：2026-03-10
- 状态：READY

## P0-A Tavily-first 执行对齐

### T01 配置结构扩展
- 文件：`nanobot/config/schema.py`
- 目标：新增 provider/fallback/tavily+brave key 结构，保留旧字段兼容。
- DoD：配置可向后兼容，`config doctor` 可显示解析结果。

### T02 WebSearchTool provider-aware
- 文件：`nanobot/agent/tools/web.py`
- 目标：支持 Tavily-first + Brave fallback。
- DoD：返回结果包含 provider_used/fallback_used。

### T03 主 Agent 注入改造
- 文件：`nanobot/cli/commands.py`、`nanobot/agent/loop.py`
- 目标：由单一 brave_api_key 改为统一 web_search_config 注入。
- DoD：主链路 web_search 默认 provider 可配置。

### T04 子 Agent 注入改造
- 文件：`nanobot/agent/subagent.py`
- 目标：子代理继承同一搜索 provider 策略。
- DoD：主/子 agent 行为一致。

### T05 治理映射对齐
- 文件：`nanobot/capability/sync.py`
- 目标：capability meta 记录默认 provider。
- DoD：`capability status` 可见默认执行策略信息。

## P0-B 资源边界与入口一致性

### T06 secrets helper 抽取
- 文件：`deploy/runtime/load_secrets_env.sh`（新增）
- 目标：统一 secrets.env 加载逻辑。
- DoD：helper 可被多入口脚本复用。

### T07 ops_check 接入 helper
- 文件：`deploy/chimera_ops_check.sh`
- 目标：ops_check 统一加载 profile secrets。
- DoD：smoke/daily/weekly 行为一致。

### T08 s8_acceptance 接入 helper
- 文件：`deploy/s8-ops-pack/chimera_s8_acceptance.sh`
- 目标：S8 验收入口和 ops_check 同口径。
- DoD：能显示已加载/未加载 secrets 的统一日志。

### T09 daily_readiness 对齐
- 文件：`deploy/chimera_daily_readiness.sh`
- 目标：与 helper 逻辑一致，避免多实现分叉。
- DoD：三入口同一加载语义。

### T10 资源能力基线文档
- 文件：`docs/OPS_QUICK_REFERENCE.md`（或新增 `docs/RESOURCE_BOUNDARY_BASELINE.md`）
- 目标：定义 workspace/missionWorkspace/memory/SSD 规则。
- DoD：规则可被运维直接执行。

### T11 missionWorkspace 优先级检查
- 文件：`nanobot/agent/subagent.py`、`nanobot/context/assembler.py`
- 目标：明确 missionWorkspace 优先级与回退规则。
- DoD：日志/metadata 可观测当前生效路径。

### T12 巡检命令补强
- 文件：`deploy/chimera_ops_check.sh`
- 目标：增加 provider 与资源边界检查输出。
- DoD：日检可直接判定“是否同口径”。

## 测试与收口

### T13 测试补齐
- 文件：`tests/test_web_search_tavily.py`、`tests/test_capability_checker.py`、`tests/test_deploy_profile_soul_sync.py`（必要时新增）
- 目标：覆盖 Tavily-first、fallback、入口脚本一致性。
- DoD：核心测试通过。

### T14 文档回填
- 文件：`docs/Issue-Checks/2026-03/56-Checks-P0-能力与资源治理-v1-2026-03-10.md`、`docs/Issue-Checks/2026-03/00-INDEX-2026-03.md`
- 目标：回填执行结果、风险与回滚验证结论。
- DoD：索引状态推进到 DONE/CHECK。
