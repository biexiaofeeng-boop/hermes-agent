# 验收清单：P0 能力与资源治理（v1）

- 日期：2026-03-10
- 状态：DONE

## C01 Tavily-first 默认策略
- [x] `web_search` 默认 provider 为 Tavily
- [x] 无 Tavily key 时按配置触发 fallback 或显式失败
- [x] 返回结果包含 provider_used/fallback_used

## C02 治理与执行同口径
- [x] `toolchain status/check` 显示与执行默认一致
- [x] `capability status/check` 可见搜索默认策略信息
- [x] 不再出现“ready 但默认执行路径不一致”

## C03 secrets 注入一致性
- [x] `chimera_ops_check.sh` 与 `chimera_s8_acceptance.sh` 一致加载 secrets
- [x] `chimera_daily_readiness.sh` 与上述入口同口径
- [x] 日检日志可明确显示 secrets 加载状态

## C04 资源边界基线
- [x] missionWorkspace 优先级规则明确且可观测
- [x] workspace/memory/SSD 规则文档化
- [x] 边界异常有明确提示与回滚路径

## C05 回归与稳定性
- [x] 不破坏 S7.3 已有稳定性主链
- [x] 不改变 AuthGate 语义边界
- [x] prod/test profile 均可通过最小巡检

## C06 最小命令集
- [x] `python3.11 -m py_compile nanobot/config/schema.py nanobot/agent/tools/web.py nanobot/agent/loop.py nanobot/agent/subagent.py nanobot/capability/sync.py`
- [x] `python3.11 -m unittest tests.test_web_search_tavily tests.test_capability_checker tests.test_taskops_feasibility -v`
- [x] `bash deploy/chimera_ops_check.sh smoke --profile test`
- [x] `bash deploy/chimera_ops_check.sh smoke --profile prod`
- [x] `bash deploy/s8-ops-pack/chimera_s8_acceptance.sh --profile test --no-health`

## 回填结论（2026-03-10）
- 验收结论：`DONE`
- 关键验证命令与结果：
  - `PYTHONPYCACHEPREFIX=/tmp/pycache-chimera-p0 python3.11 -m py_compile ...` -> PASS
  - `PYTHONPYCACHEPREFIX=/tmp/pycache-chimera-p0 python3.11 -m unittest ...` -> PASS（33 tests, OK）
  - `bash deploy/chimera_ops_check.sh smoke --profile test` -> PASS（含 provider + 资源边界巡检输出）
  - `bash deploy/chimera_ops_check.sh smoke --profile prod` -> PASS（含 provider + 资源边界巡检输出）
  - `bash deploy/s8-ops-pack/chimera_s8_acceptance.sh --profile test --no-health` -> PASS（fallback Python + 无回滚备份时跳过 dry-run）
- 残留风险：
  - 当前 `websearch-tavily` 在 toolchain 状态为 disabled；默认 provider 已切 Tavily，但无 key 时将进入 fallback/报错路径（属预期配置行为）。
