# 实施顺序脚本清单：S8（v1）

- 日期：2026-03-06
- 状态：READY

## 0) 基线同步
```bash
cd /Users/sourcefire/1data/X-Chimera/chimera-core
git fetch origin
git checkout -b codex/s8-config-skills-governance-v1 origin/master
```

## 1) 文档先行（必读）
```bash
# 任务包 + 验收 + 运营手册
cat docs/Issue-Checks/2026-03/41-TaskPackage-S8-Config-Skills-Governance-v1-2026-03-06.md
cat docs/Issue-Checks/2026-03/42-Checks-S8-Config-Skills-Governance-v1-2026-03-06.md
cat docs/Issue-Checks/2026-03/44-运营手册-S8-Config-Skills-v1-2026-03-06.md
```

## 2) 生产迁移脚本预演（S8-OPS）
```bash
# 仅预览，不写入
bash deploy/s8-migration/chimera_s8_migrate.sh plan --profile prod --dry-run

# 查看当前状态与备份计数
bash deploy/s8-migration/chimera_s8_migrate.sh status --profile prod

# 回滚脚本可用性检查
bash deploy/s8-migration/chimera_s8_rollback.sh list --profile prod
```

## 3) 阶段1（Config 边界收敛）
```bash
# 开发后最小检查
python -m py_compile nanobot/config/schema.py nanobot/config/loader.py nanobot/cli/commands.py

# 现有能力诊断（基线）
python -m nanobot.cli.commands skills list
python -m nanobot.cli.commands skills doctor
```

## 4) 阶段2（Secret Ref 迁移）
```bash
# 开发后最小检查
python -m py_compile nanobot/config/schema.py nanobot/cli/commands.py

# 新增 secret 解析测试（依赖 pydantic）
python -m unittest tests.test_config_secret_resolution -v

# 迁移诊断（查看 legacy 明文提示）
python -m nanobot.cli.commands config doctor --show-sensitive
```

## 5) 阶段3（Skills 生命周期）
```bash
# 开发后最小检查
python -m py_compile nanobot/agent/skills.py nanobot/cli/commands.py nanobot/skill_registry.py

# 回归现有命令
python -m nanobot.cli.commands skills list
python -m nanobot.cli.commands skills doctor
python -m nanobot.cli.commands skills list-registry
python -m unittest tests.test_skill_registry tests.test_skills_loader -v
python -m nanobot.cli.commands capability sync --from skills
```

## 6) 合并前回归
```bash
bash deploy/chimera_core_test.sh
```

## 7) 文档回填
```bash
# 回填：
# docs/Issue-Checks/2026-03/42-Checks-S8-Config-Skills-Governance-v1-2026-03-06.md
# docs/Issue-Checks/2026-03/00-INDEX-2026-03.md
```
