# 分支对比与闭环修复任务单（2026-03-03）

- 状态：DONE（2026-03-03）

## 1) 对比基线
- 基线分支：`master`
- 修复分支：`codex/prod-runtime-sync-20260303`
- 修复分支收口：`3a63108`
- 运行期快照主提交：`a5c4615`

## 2) 与 master 的差异（本地核对）
- 新增：
  - `deploy/chimera_daily_readiness.sh`
  - `docs/PROD_RELEASE_STEP_BY_STEP.md`
  - `docs/Issue-Checks/2026-03/runtime/01-运行态本地改动与风险说明-2026-03-03.md`
  - `nanobot/skills/prod-release-ops/SKILL.md`
  - `nanobot/skills/web-search/tavily_run.py`
- 修改：
  - `deploy/chimera_watchdog.sh`
  - `nanobot/agent/loop.py`
  - `nanobot/skills/web-search/SKILL.md`

## 3) 发现的问题（需要闭环）

### P0
1. `tavily_run.py` 语法错误（阻断执行）
   - 现象：`python -m py_compile nanobot/skills/web-search/tavily_run.py` 报 `SyntaxError`。
   - 结论：字符串换行写法导致 f-string 未闭合。

### P1
2. `loop.py` 歧义绕过词过宽
   - 现状：包含口语词（如“宝子们”“直接执行”）即可绕过。
   - 风险：误触发执行链。
   - 建议：仅保留结构化前缀（`task:` / `execute:`）并补测试。

3. 分支落后 `master` 1 个稳定性修复提交
   - `master` 独有提交：`2726a71`（deploy/watchdog 稳定性修复）。
   - 风险：PR 直接合并可能回滚主线已修复行为。

### P2
4. `web-search/SKILL.md` 示例代码换行不规范
  - 建议：修正为可复制执行的代码块，避免误导运行时脚本生成。

## 3.1) 闭环结果（2026-03-03）
- P0 已修复：`nanobot/skills/web-search/tavily_run.py` 字符串换行改为 `\\n` 形式，`py_compile` 通过。
- P1 已修复：`nanobot/agent/loop.py` 歧义绕过仅保留结构化前缀 `task:` / `execute:`，口语触发不再绕过。
- P1 测试已补：`tests/test_project_context_router.py::test_ambiguous_instruction_bypass_requires_structured_prefix`。
- P2 已修复：`nanobot/skills/web-search/SKILL.md` 示例代码改为可直接复制执行格式。
- 补充语法测试：`tests/test_web_search_tavily.py`（校验 `tavily_run.py` 可编译）。

## 4) 给 chimera-core-codex 的实施顺序（闭环）
1. **先同步主线**：在 `codex/prod-runtime-sync-20260303` 上 rebase `master`。
2. **修语法**：修复 `nanobot/skills/web-search/tavily_run.py`，确保 `py_compile` 通过。
3. **收紧绕过**：调整 `nanobot/agent/loop.py`，去掉口语绕过，仅保留结构化触发。
4. **补测试**：新增/更新对应单测（至少覆盖：
   - 口语不绕过
   - `task:`/`execute:` 可绕过
   - tavily 脚本可编译）。
5. **文档对齐**：同步更新 `runtime/01` 与本文件结论。

## 5) 验收标准
- [x] `python -m py_compile nanobot/skills/web-search/tavily_run.py` 通过
- [x] `deploy/chimera_core_test.sh` 通过（`Ran 172 tests, OK (skipped=3)`）
- [x] `loop.py` 绕过规则有测试且可解释
- [x] PR 基于最新 `master`，不回滚稳定修复提交

## 5.1) 验收命令记录
```bash
/Users/sourcefire/X-lab/chimera-core-prod/.venv/bin/python -m py_compile nanobot/skills/web-search/tavily_run.py
/Users/sourcefire/X-lab/chimera-core-prod/.venv/bin/python -m unittest tests.test_project_context_router tests.test_web_search_tavily -v
PYTHON_BIN=/Users/sourcefire/X-lab/chimera-core-prod/.venv/bin/python bash deploy/chimera_core_test.sh
```

## 6) PR 入口
- 分支地址：<https://github.com/biexiaofeeng-boop/chimera-core/tree/codex/prod-runtime-sync-20260303>
- PR 入口：<https://github.com/biexiaofeeng-boop/chimera-core/pull/new/codex/prod-runtime-sync-20260303>
