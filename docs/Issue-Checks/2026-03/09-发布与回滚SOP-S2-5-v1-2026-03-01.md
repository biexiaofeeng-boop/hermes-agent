# 发布与回滚SOP：S2-5 OODA + Armory

- 任务ID: S2-5
- 日期: 2026-03-01
- 状态: CHECK（待 D02/D03）

## 发布前门禁

1. `06-Checks-S2-5-OODA-Armory-v1-2026-03-01.md` 中 A/B/D01/E 全绿。
2. `bash deploy/chimera_core_test.sh` 全量回归通过。
3. D02（失败率趋势）与 D03（24h 稳态）完成窗口采样。
4. 发布必须基于远端快照：先 `git fetch --all --prune`，再确认目标提交。

## 发布步骤

1. 合并前校验：
   - `bash deploy/chimera_core_test.sh`
   - `python -m nanobot.cli.commands skills list`
   - `python -m nanobot.cli.commands capability sync --from skills`
   - `python -m nanobot.cli.commands auth stats --limit 200`
2. 合并与推送：
   - 以 `codex/s2-5-ooda-armory-compat` 为集成分支收口后并入 `master`
   - 推送 `master` 到远端
3. 发布后冒烟：
   - `python -m nanobot.cli.commands status`
   - `python -m nanobot.cli.commands skills doctor --limit 20`
   - `python -m nanobot.cli.commands capability list --type skill`

## 回滚触发条件

1. 授权失败分类中 `route` 或 `race` 快速升高并影响主链。
2. `skills doctor` 持续出现关键技能缺失，导致 capability blocked 激增。
3. OODA 响应明显偏离模板导致流程无法推进。

## 回滚步骤

1. 回退到最近稳定 tag / 提交并强制同步部署环境。
2. 若问题来自配置，先回滚 `skills` / `auth` 配置，再恢复服务。
3. 回滚后执行：
   - `bash deploy/chimera_core_test.sh`
   - `python -m nanobot.cli.commands auth stats --limit 200`
4. 在 `00-INDEX-2026-03.md` 记录回滚原因、影响面、修复窗口。
