# 任务卡：S9 T01-T18

- 日期：2026-03-29
- 状态：READY
- 关联：`01-TaskPackage-S9-Chimera-Ironelf-RuntimeBridge-v1-2026-03-29.md`

## A. 路由与配置（P0）

## T01

- 输出：runtime lane 配置模型
- DoD：支持 `enabled/base_url(or socket)/timeout/health_ttl/failopen`

## T02

- 输出：lane 选择规则
- DoD：仅高风险、长耗时、隔离型任务进入 runtime lane

## T03

- 输出：bridge health cache
- DoD：短时缓存探活结果，避免每轮重复探活拖慢对话

## B. 请求与协议适配（P0）

## T04

- 输出：`ExecutionRequest` builder
- DoD：包含 `trace_id/task_id/execution_id/lane/risk_level/objective/tool_hints`

## T05

- 输出：bridge client
- DoD：支持 submit / stream(or poll) / cancel / health

## T06

- 输出：协议版本适配层
- DoD：未知字段忽略，结构化错误可上抛

## C. 容灾回退（P0）

## T07

- 输出：启动期降级
- DoD：health 失败时自动不走 runtime lane

## T08

- 输出：派发期降级
- DoD：submit 失败时转本地路径或返回明确 fallback 回执

## T09

- 输出：执行中断兜底
- DoD：event stream 中断时给出 runtime degraded 总结，不静默挂住

## T10

- 输出：成功宣称保护扩展
- DoD：没有 runtime receipt/evidence 时不能宣称“已执行成功”

## D. UX 与任务治理（P1）

## T11

- 输出：runtime 事件到 `trace/taskops` 的映射
- DoD：关键事件写回 trace store，必要时更新 task 状态

## T12

- 输出：`FinalReport` 组装策略
- DoD：把 runtime 结果转成 `chimera-core` 风格回执，不暴露底层噪音

## T13

- 输出：`TaskConfirm` 与 runtime lane 联动规则
- DoD：确认后才派发的任务仍保持现有确认体验

## T14

- 输出：取消与中断控制
- DoD：复杂任务可从主线程触发 runtime cancel

## E. 测试与回填（P0）

## T15

- 输出：桥接 client 单测
- DoD：覆盖 health/submit/stream/cancel/fallback

## T16

- 输出：对话与容灾回归测试
- DoD：覆盖 fast lane、runtime lane、runtime down、receipt 缺失

## T17

- 输出：checks 回填
- DoD：`03-Checks-S9-RuntimeBridge-v1-2026-03-29.md` 证据完整

## T18

- 输出：索引回填
- DoD：主索引登记完成
