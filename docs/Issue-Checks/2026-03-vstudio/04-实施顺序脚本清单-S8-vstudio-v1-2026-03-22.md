# 实施顺序脚本清单：S8-vstudio（v1）

- 日期：2026-03-22
- 状态：READY

## 1) 基线同步

```bash
git fetch origin
git checkout codex/vstudio-s8-control-data-plane-v1 || git checkout -b codex/vstudio-s8-control-data-plane-v1
git rebase origin/master
```

## 2) 文档与schema落地

```bash
# 先落地 schema 与文档，再进入 adapter 开发
# （具体文件按任务卡 T01~T04）
```

## 3) 影子写入联调

```bash
# 仅启用 write-path，不启用 read-path
# 验证事件日志可写、主链不受影响
```

## 4) 回测与验收

```bash
# 单测 + 集成回测 + 运行手工冒烟
# 回填 checks 文档
```

## 5) 收口

```bash
# 提交变更
# 更新 00-INDEX-2026-03-vstudio.md
```
