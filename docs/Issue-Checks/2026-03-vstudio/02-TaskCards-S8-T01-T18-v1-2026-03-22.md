# 任务卡：S8 VStudio（T01~T18）

- 日期：2026-03-22
- 状态：READY
- 关联：`01-TaskPackage-S8-VStudio-ControlDataPlane-v1-2026-03-22.md`

## A. 协议与存储（P0）

## T01
- 输出：`TaskEnvelope` schema（json schema + 示例）
- DoD：字段、状态流、版本号定义完整。

## T02
- 输出：`EventRecord` schema（append-only）
- DoD：支持 `event_id/node_id/trace_id/task_id` 与幂等键。

## T03
- 输出：`CorpusRecord` schema
- DoD：包含 `instruction/input/output/timestamp/quality/source`。

## T04
- 输出：Data Plane 目录结构与命名规范
- DoD：`events/`, `materialized/`, `corpus/`, `snapshots/` 明确。

## B. 适配器与写入链（P0/P1）

## T05
- 输出：Cherry 适配器（事件写入）设计与接口清单
- DoD：不侵入核心存储，失败不阻断。

## T06
- 输出：chimera-core 适配器（事件写入）设计与接口清单
- DoD：TaskOps/skills 执行事件可记录。

## T07
- 输出：统一 `trace_id/task_id` 贯通策略
- DoD：跨系统链路可关联。

## T08
- 输出：故障降级策略（fail-open）
- DoD：中间层失败不影响主链回复/执行。

## C. 检索与语料（P1）

## T09
- 输出：检索接口（timeline/tag/vector）设计
- DoD：只读查询，不驱动执行。

## T10
- 输出：日增量语料抽取流程
- DoD：支持按天写入 `training_corpus_YYYY.jsonl`。

## T11
- 输出：语料质量门槛规则
- DoD：仅 `evidence=true` 或 `human_approved=true` 入主语料。

## D. 路由策略（P1）

## T12
- 输出：`routing_policy` 配置草案
- DoD：按任务类型/成本/质量定义模型池与 fallback。

## T13
- 输出：路由可观测字段
- DoD：记录选模原因、降级原因、预算命中情况。

## E. 发布与治理（P0）

## T14
- 输出：独立 profile 部署方案（vstudio）
- DoD：端口、日志、runtime_home 独立。

## T15
- 输出：灰度策略（影子写 -> 只读 -> 小流量）
- DoD：每阶段可开关、可回滚。

## T16
- 输出：运维检查表
- DoD：健康检查、回放检查、性能基线检查完整。

## F. 文档回填（P0）

## T17
- 输出：验收证据回填
- DoD：`03-Checks-S8-VStudio-v1-2026-03-22.md` 完整更新。

## T18
- 输出：索引回填
- DoD：`00-INDEX-2026-03-vstudio.md` 与主索引入口同步。
