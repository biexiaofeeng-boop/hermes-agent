# Hermes Production Check - 2026-07-04

Production root: `/Users/sourcefire/X-lab/chimera-hermes-agent`

Runtime profile: `/Users/sourcefire/X-lab/chimera-hermes-agent/.runtime/hermes-profiles/eval`

Launchd service: `/Users/sourcefire/Library/LaunchAgents/ai.hermes.gateway.plist`

## Release

- Before: `dde7d4dc5` / Hermes Agent `v0.16.0`
- After: `88ce579c0` / Hermes Agent `v0.17.0`
- Update method: `git fetch origin` + `git merge --ff-only origin/main`
- Gateway restart method: `launchctl bootout` + `launchctl bootstrap`

## Checks

- Git state synchronized with `origin/main`: yes
- Launchd plist valid via `plutil -lint`: yes
- Gateway launchd service running: yes
- Gateway PID after restart: `28250`
- Production `config.yaml` retained:
  - `model.provider: custom:dmx`
  - `model.default: gpt-5.4`
  - `telegram.proxy_url: http://127.0.0.1:7897`
  - platforms: `telegram`, `feishu`
  - external skills: `~/X-lab/chimera-skills`
- Production `SOUL.md` retained: yes
- Core imports checked:
  - `gateway.run`
  - `plugins.platforms.telegram.adapter`
  - `plugins.platforms.feishu.adapter`
  - `run_agent`
- Telegram smoke send: passed
- DMX `/chat/completions` smoke request through proxy: passed

## Notes

- Telegram adapter moved to bundled platform plugin namespace in v0.17.0.
- Launchd plist is intentionally kept instead of regenerating with `hermes gateway start`, because it contains the production proxy environment:
  - `HTTPS_PROXY=http://127.0.0.1:7897`
  - `HTTP_PROXY=http://127.0.0.1:7897`
  - `ALL_PROXY=http://127.0.0.1:7897`
  - `NO_PROXY=localhost,127.0.0.1,::1`
- `hermes gateway status` reports the service definition as stale relative to the current install. This is expected for the current production override, but should be kept in mind if regenerating the launchd plist.
- Feishu still reports `app_id is invalid` from Lark SDK. This predates the release and is a Feishu app credential/config issue, not a v0.17.0 regression.

## Restart Script

Use:

```bash
/Users/sourcefire/X-lab/chimera-hermes-agent/docs/ops/restart-hermes-gateway.sh
```
